{
  "name": "ens-tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "abi:sync": "node scripts/sync-abi.mjs"
  },
  "dependencies": {
    "viem": "^2.37.13"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.2",
    "typescript": "^5.7.3",
    "undici": "^7.24.6",
    "vite": "^6.1.1",
    "vitest": "^4.1.2"
  }
}
